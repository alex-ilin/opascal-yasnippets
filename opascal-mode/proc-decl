# -*- mode: snippet -*-
# name: proc-declaration
# key: pd
# --
procedure ${1:`(if (stringp (get-register ?i))
  (get-register ?i)
  "Identifier-or-register-i")`}(${2:`(if (stringp (get-register ?p))
  (get-register ?p)
  "Paremeters-or-register-p")`});
